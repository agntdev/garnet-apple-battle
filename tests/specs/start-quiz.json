[
  {
    "name": "/start_quiz",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Доступ администратора ещё не настроен."
            }
          }
        ],
        "send": {
          "text": "/start_quiz"
        }
      }
    ]
  },
  {
    "name": "Add questions remains an admin-only append entry point",
    "steps": [
      {
        "send": { "callback": "quiz:setup" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Доступ администратора ещё не настроен."
            }
          }
        ]
      }
    ]
  }
]
