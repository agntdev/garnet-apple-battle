[
  {
    "name": "Join via QR Code",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "That quiz link isn't valid or has expired. Scan the current event QR code and try again."
            }
          }
        ],
        "send": {
          "callback": "join:qr"
        }
      }
    ]
  }
]
