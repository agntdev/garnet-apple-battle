[
  {
    "name": "Join via QR Code",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Participant joins quiz session using a one-time QR code token"
            }
          }
        ],
        "send": {
          "callback": "join:qr"
        }
      }
    ]
  }
]
